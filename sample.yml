---
- hosts: all
  become: yes
  gather_facts: yes
  tasks:
   - name : i  want to install webserver on ubuntu nodes
     apt:
      name: apache2
      state: present
   - name: 
     service: 
       name: apache2
       sate: started
     when: ansible_os_family =="Debain"      