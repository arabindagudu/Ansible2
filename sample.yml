---
- hosts: all
  become: yes
  gather_facts: yes
  tasks:
   - name : i  want to install webserver on ubuntu nodes
     apt:
      name: nginx
      state: present
     when: ansible_os_family =="Debain"   
   - name: 
     service: 
       name: nginx
       sate: started
     when: ansible_os_family =="Debain"      